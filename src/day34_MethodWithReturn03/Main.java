package day34_MethodWithReturn03;

public class Main {

	public static void main (String [] args) {
	//access modifiers 
	/*
	 * if we have a java code the following process happens: 
	 * - Java Code >>> Compile >>> Run, 
	 * sometimes the code compiles, but not run 
	 * if a code compiles and doesn't run, then call that class from the main method
	 * if we put ... in main method instead of [], it will work, but it is limited 
	 */
		
		System.out.println("Main method test");
		
	}
}